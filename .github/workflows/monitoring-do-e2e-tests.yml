name: Synthetic DO Infrastructure Monitoring tests

# This workflow validates the DigitalOcean global infrastructure endpoints
# by running e2e tests against a Vercel preview configured to use DO RPC/LCD/SQS.
# Used for migration validation before shifting traffic from GCP to DO.

on:
  workflow_dispatch:
    inputs:
      skip_server_tests:
        description: 'Skip server synthetic tests'
        required: false
        default: 'false'
        type: boolean
  schedule:
    # Run every 30 minutes for continuous validation
    - cron: "*/30 * * * *"

# DO endpoint configuration
env:
  DO_BRANCH: "jason/do-endpoints-e2e"
  DO_LCD_ENDPOINT: "https://lcd-do-glb.osmosis.zone"
  DO_SQS_ENDPOINT: "https://sqs-do-glb.osmosis.zone"
  DO_RPC_ENDPOINT: "https://rpc-do-glb.osmosis.zone"

jobs:
  # Resolve the latest READY Vercel preview URL for the DO branch
  resolve-do-preview:
    runs-on: ubuntu-latest
    outputs:
      environment_url: ${{ steps.vercel.outputs.environment_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Get DO branch Vercel preview URL
        id: vercel
        env:
          BRANCH_NAME: ${{ env.DO_BRANCH }}
          FILTER_BRANCH: ${{ env.DO_BRANCH }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_PROJECT: ${{ secrets.VERCEL_PROJECT }}
        run: |
          echo "Looking for latest READY deployment for branch: $FILTER_BRANCH"
          cd .github && python await_deployment.py
      - name: Echo resolved URL
        env:
          environment_url: ${{ steps.vercel.outputs.environment_url }}
        run: |
          echo "DO Preview URL: https://$environment_url"
          echo "This preview is configured to use DO endpoints via Vercel branch env vars"

  # Server synthetic tests against DO SQS
  server-e2e-tests:
    name: do-server-tests
    needs: resolve-do-preview
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.skip_server_tests != 'true' }}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Echo DO SQS Server URL
        run: echo "Testing SQS Server URL: ${{ env.DO_SQS_ENDPOINT }}"

      - name: Run Server Tests against DO SQS
        id: tests
        run: yarn test:e2e --filter=@osmosis-labs/server
        env:
          NEXT_PUBLIC_SIDECAR_BASE_URL: ${{ env.DO_SQS_ENDPOINT }}
          NEXT_PUBLIC_TIMESERIES_DATA_URL: https://data.app.osmosis.zone

  # FE swap monitoring tests (US region - no proxy)
  fe-swap-us:
    timeout-minutes: 15
    name: do-fe-swap-us
    needs: resolve-do-preview
    runs-on: macos-14
    steps:
      - name: Echo IP
        run: curl -L "https://ipinfo.io" -s
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Swap tests (US) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_3 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_3 }}
        run: |
          echo "Testing DO preview at: $BASE_URL"
          cd packages/e2e
          npx playwright test monitoring.swap
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-us-swap-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE swap monitoring tests (EU region via proxy)
  fe-swap-eu:
    timeout-minutes: 15
    name: do-fe-swap-eu
    needs: resolve-do-preview
    runs-on: macos-14
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Swap tests (EU) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          TEST_PROXY: "http://138.68.112.16:8888"
          TEST_PROXY_USERNAME: ${{ secrets.TEST_PROXY_USERNAME }}
          TEST_PROXY_PASSWORD: ${{ secrets.TEST_PROXY_PASSWORD }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_2 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_2 }}
          USE_TEST_PROXY: "use"
        run: |
          echo "Testing DO preview at: $BASE_URL (via EU proxy)"
          cd packages/e2e
          npx playwright test monitoring.swap
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-eu-swap-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE swap monitoring tests (SG/Asia region via proxy)
  fe-swap-sg:
    timeout-minutes: 15
    name: do-fe-swap-sg
    needs: resolve-do-preview
    runs-on: macos-14
    steps:
      - name: Echo IP
        run: curl -L "https://ipinfo.io" -s
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Swap tests (SG) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          TEST_PROXY: "http://139.59.218.19:8888"
          TEST_PROXY_USERNAME: ${{ secrets.TEST_PROXY_USERNAME }}
          TEST_PROXY_PASSWORD: ${{ secrets.TEST_PROXY_PASSWORD }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_1 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_1 }}
          USE_TEST_PROXY: "use"
        run: |
          echo "Testing DO preview at: $BASE_URL (via SG proxy)"
          cd packages/e2e
          npx playwright test monitoring.swap
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-sg-swap-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE market order tests (US region)
  fe-trade-us:
    timeout-minutes: 12
    needs: [resolve-do-preview, fe-swap-us]
    name: do-fe-trade-us
    runs-on: macos-14
    outputs:
      unexpected: ${{ steps.set-output.outputs.unexpected }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Market tests (US) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_3 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_3 }}
        run: |
          cd packages/e2e
          npx playwright test monitoring.market --timeout 90000
      - name: set-output
        if: failure() || success()
        id: set-output
        run: echo "unexpected=$(jq -r '.stats.unexpected' packages/e2e/playwright-report/test-results.json)" >> $GITHUB_OUTPUT
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-us-trade-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE market order tests (EU region)
  fe-trade-eu:
    timeout-minutes: 12
    needs: [resolve-do-preview, fe-swap-eu]
    name: do-fe-trade-eu
    runs-on: macos-14
    outputs:
      unexpected: ${{ steps.set-output.outputs.unexpected }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Market tests (EU) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          TEST_PROXY: "http://138.68.112.16:8888"
          TEST_PROXY_USERNAME: ${{ secrets.TEST_PROXY_USERNAME }}
          TEST_PROXY_PASSWORD: ${{ secrets.TEST_PROXY_PASSWORD }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_2 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_2 }}
          USE_TEST_PROXY: "use"
        run: |
          cd packages/e2e
          npx playwright test monitoring.market --timeout 180000
      - name: set-output
        if: failure() || success()
        id: set-output
        run: echo "unexpected=$(jq -r '.stats.unexpected' packages/e2e/playwright-report/test-results.json)" >> $GITHUB_OUTPUT
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-eu-trade-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE limit order tests (US region)
  fe-limit-us:
    timeout-minutes: 15
    if: failure() || success()
    needs: [resolve-do-preview, fe-trade-us]
    name: do-fe-limit-us
    runs-on: macos-14
    outputs:
      unexpected: ${{ steps.set-output.outputs.unexpected }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Limit tests (US) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_3 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_3 }}
        run: |
          cd packages/e2e
          npx playwright test monitoring.limit --timeout 180000
      - name: set-output
        if: failure() || success()
        id: set-output
        run: echo "unexpected=$(jq -r '.stats.unexpected' packages/e2e/playwright-report/test-results.json)" >> $GITHUB_OUTPUT
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-us-limit-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # FE limit order tests (EU region)
  fe-limit-eu:
    timeout-minutes: 15
    if: failure() || success()
    needs: [resolve-do-preview, fe-trade-eu]
    name: do-fe-limit-eu
    runs-on: macos-14
    outputs:
      unexpected: ${{ steps.set-output.outputs.unexpected }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          sparse-checkout: |
            packages/e2e
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: "**/node_modules"
          key: ${{ runner.OS }}-22.x-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.OS }}-22.x-
      - name: Install Playwright
        run: |
          yarn --cwd packages/e2e install --frozen-lockfile && npx playwright install --with-deps chromium
      - name: Run Limit tests (EU) against DO preview
        env:
          BASE_URL: "https://${{ needs.resolve-do-preview.outputs.environment_url }}"
          REST_ENDPOINT: ${{ env.DO_LCD_ENDPOINT }}
          TEST_PROXY: "http://138.68.112.16:8888"
          TEST_PROXY_USERNAME: ${{ secrets.TEST_PROXY_USERNAME }}
          TEST_PROXY_PASSWORD: ${{ secrets.TEST_PROXY_PASSWORD }}
          PRIVATE_KEY: ${{ secrets.TEST_PRIVATE_KEY_2 }}
          WALLET_ID: ${{ secrets.TEST_WALLET_ID_2 }}
          USE_TEST_PROXY: "use"
        run: |
          cd packages/e2e
          npx playwright test monitoring.limit --timeout 180000
      - name: set-output
        if: failure() || success()
        id: set-output
        run: echo "unexpected=$(jq -r '.stats.unexpected' packages/e2e/playwright-report/test-results.json)" >> $GITHUB_OUTPUT
      - name: upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: do-eu-limit-test-results-${{ github.run_id }}
          path: packages/e2e/playwright-report

  # Alert on critical failures (2nd attempt with multiple failures)
  fe-bot-alert:
    runs-on: ubuntu-latest
    needs:
      [
        server-e2e-tests,
        fe-trade-eu,
        fe-trade-us,
        fe-limit-us,
        fe-limit-eu,
      ]
    if: failure() && github.run_attempt == 2 && (needs.fe-trade-eu.outputs.unexpected > 1 || needs.fe-trade-us.outputs.unexpected > 1 || needs.fe-limit-eu.outputs.unexpected > 1 || needs.fe-limit-us.outputs.unexpected > 1)
    steps:
      - name: Install Datadog CI
        run: |
          echo "Installing Datadog CI..."
          curl -L --fail "https://github.com/DataDog/datadog-ci/releases/download/v4.1.2/datadog-ci_linux-x64" --output "/usr/local/bin/datadog-ci"
          chmod +x /usr/local/bin/datadog-ci
          echo "Datadog CI installed"

      - name: Verify Datadog CI Installation
        run: |
          echo "Verifying Datadog CI installation..."
          datadog-ci version
          echo "Datadog CI is ready to use"

      - name: Send Datadog alert for DO infrastructure failure
        env:
          DD_API_KEY: ${{ secrets.DATADOG_API_KEY }}
          DD_APP_KEY: ${{ secrets.DATADOG_APPLICATION_KEY }}
          DD_SITE: ${{ secrets.DATADOG_SITE }}
        run: |
          echo "Sending DO infrastructure failure metrics to Datadog..."

          # Tag as DO infrastructure failure
          datadog-ci tag --level pipeline \
            --tags "critical_failure:true" \
            --tags "infrastructure:digitalocean" \
            --tags "dd_gh_run_attempt:${{ github.run_attempt }}"

          # Add tags for unexpected failure counts by region
          datadog-ci tag --level pipeline \
            --tags "do_fe_trade_eu_unexpected:${{ needs.fe-trade-eu.outputs.unexpected }}" \
            --tags "do_fe_trade_us_unexpected:${{ needs.fe-trade-us.outputs.unexpected }}" \
            --tags "do_fe_limit_eu_unexpected:${{ needs.fe-limit-eu.outputs.unexpected }}" \
            --tags "do_fe_limit_us_unexpected:${{ needs.fe-limit-us.outputs.unexpected }}"

          echo "Metrics sent to Datadog successfully"

  # Clean up deployments
  delete-deployments:
    runs-on: ubuntu-latest
    if: always()
    needs:
      [
        server-e2e-tests,
        fe-trade-eu,
        fe-trade-us,
        fe-limit-us,
        fe-limit-eu,
        fe-bot-alert,
      ]
    steps:
      - name: Delete Previous deployments
        uses: actions/github-script@v7
        with:
          debug: true
          script: |
            const deployments = await github.rest.repos.listDeployments({
               owner: context.repo.owner,
               repo: context.repo.repo,
               sha: context.sha
             });
             await Promise.all(
               deployments.data.map(async (deployment) => {
                 await github.rest.repos.createDeploymentStatus({ 
                   owner: context.repo.owner, 
                   repo: context.repo.repo, 
                   deployment_id: deployment.id, 
                   state: 'inactive' 
                 });
                 return github.rest.repos.deleteDeployment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  deployment_id: deployment.id
                });
               })
             );
