{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        "build/**",
        ".next/**",
        "!.next/cache/**",
        "src/codegen/**",
        "scripts/generated/**",
        "config/wallet-registry.ts",
        "config/wallet-assets.ts",
        "config/chain-infos.ts"
      ],
      "dependsOn": ["^build"]
    },
    "lint": {},
    "lint:fix": {},
    "dev": { "dependsOn": ["build"], "persistent": true },
    "start": { "dependsOn": ["build"] },
    "clean": { "cache": false },
    "test": { "dependsOn": ["build"] },
    "analyze": {}
  }
}
