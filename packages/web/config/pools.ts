/** Tick spacing used by the frontend when creating a new Concentrated
 *  Liquidity pool. The chain accepts other values for governance-authorized
 *  spread factors, but the frontend always creates pools at 100. */
export const CL_POOL_CREATION_TICK_SPACING = 100;
