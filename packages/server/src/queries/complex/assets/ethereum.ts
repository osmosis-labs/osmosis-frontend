import { NativeEVMTokenConstantAddress } from "@osmosis-labs/utils";

export function getEvmBalance({ address, userAddress, chainId }: {
  address: string;
  userAddress: string;
  chainId: number;
}) {
    if(address === NativeEVMTokenConstantAddress)
}
