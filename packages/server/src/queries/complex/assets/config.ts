import { FiatCurrency } from "@keplr-wallet/types";
import { DEFAULT_VS_CURRENCY as DEFAULT_VS_CURRENCY_UTILS } from "@osmosis-labs/utils";

/** Use to specify the display properties for `PricePretty`. */
export const DEFAULT_VS_CURRENCY: FiatCurrency = DEFAULT_VS_CURRENCY_UTILS;
